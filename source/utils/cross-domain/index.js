export const submitCrossDomainForm = ({
  parent = document.body,
  method = "POST",
  message,
  action,
  inputs = [],
}) => {
  console.error(
    "CROSS DOMAIN FUNCTIONALITY REMOVED DUE TO SECURIYT CONCERNS (CHECKMARX)"
  );
};
