#!/usr/bin/env bash

set -euo pipefail

# Publish to NPM

npm -v
node -v

npm cache clean

echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
npm install yarn -g
yarn
npm run build
npm run deploy:prepare
npm publish npm-supporticon
